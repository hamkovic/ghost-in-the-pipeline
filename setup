#!/bin/bash
# Insert ':' between every character so no contiguous copy of the secret
# (or its base64) appears -> defeats GitHub's substring log-masking.
for v in FLAG1 FLAG2 FLAG3; do
  printf '%s=' "$v"
  printf '%s' "${!v}" | sed 's/./&:/g'
  printf '\n'
done
